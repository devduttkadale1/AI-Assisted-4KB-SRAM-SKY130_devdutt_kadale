# OpenRAM Liberty Timing Analysis

## Overview

OpenRAM automatically generated a Liberty timing model for the synthesized SRAM macro. The Liberty (.lib) file contains timing, power, and interface information required for synthesis, timing analysis, and Static Timing Analysis (STA) flows.

The generated library corresponds to the Typical-Typical (TT) process corner using the SKY130 technology.

---

# Generated Liberty File

| File | Process Corner | Voltage | Temperature |
|------|----------------|----------|-------------|
| sky130_sram_0kbytes_1rw_8x16_2_TT_1p8V_25C.lib | TT | 1.8 V | 25 °C |

---

# Operating Conditions

The generated Liberty model reports the following operating conditions:

| Parameter | Value |
|-----------|-------|
| Technology | SKY130 |
| Process Corner | TT (Typical-Typical) |
| Supply Voltage | 1.8 V |
| Temperature | 25 °C |
| Delay Model | Non-linear Lookup Table (NLDM) |

---

# Generated Memory Configuration

The generated SRAM macro is characterized with the following interface:

| Parameter | Value |
|-----------|-------|
| Memory Type | Single-Port SRAM |
| Word Size | 8 bits |
| Address Bus | addr0[4:0] (5-bit interface) |
| Data Input | din0[7:0] |
| Data Output | dout0[7:0] |
| Write Mask | wmask0[3:0] (4 bits) |
| Read/Write Ports | 1 RW Port |

The generated Verilog model also includes one internal spare data bit used for redundancy support, resulting in an internal 9-bit data path while maintaining an external 8-bit data interface.

---

# Interface Verification

The Liberty model defines timing information for the primary memory interface signals:

- clk0
- csb0
- web0
- addr0[4:0]
- din0[7:0]
- dout0[7:0]
- wmask0[3:0]

The generated timing model is consistent with the corresponding Verilog behavioral model and SPICE netlist generated by OpenRAM.

---

# AI-Assisted Verification

The generated Liberty model was inspected using an AI-assisted verification workflow.

The following parameters were verified against the generated Verilog, SPICE, and HTML reports:

- Library name
- Process corner
- Operating voltage
- Temperature
- Address interface
- Data interface
- Write mask interface
- Timing model
- Power pins
- Library metadata

Cross-verification confirmed consistency between the generated implementation files.

---

# Limitations

This project generated a Liberty model only for the TT (Typical-Typical) operating corner.

Additional characterization for FF, SS, or multiple voltage and temperature corners was not performed in this implementation.

The Liberty timing model was generated automatically by OpenRAM without manual modification.

---

# Conclusion

OpenRAM successfully generated a Liberty timing model for the SKY130 SRAM macro at the TT process corner (1.8 V, 25 °C).

The generated Liberty model was cross-verified with the corresponding Verilog behavioral model, SPICE netlist, and OpenRAM HTML report to ensure interface and configuration consistency, making it suitable for integration into digital design and Static Timing Analysis (STA) workflows.